.site-header {
  height: inherit;
  padding-top: 0; }
  .site-header nav {
    margin-left: auto;
    display: flex;
    width: 40%;
    justify-content: space-between; }
    .site-header nav a {
      color: #70cf32;
      font-weight: 700;
      text-transform: uppercase; }
    .site-header nav .active {
      color: #889096; }
  .site-header .burger {
    margin-left: auto;
    font-size: 28px; }
